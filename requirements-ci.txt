# CI-specific dependencies
pytest>=7.1.2
pytest-cov>=4.1.0
flake8>=7.1.2
coverage>=7.3.2

# Core dependencies (subset of requirements.txt that works well in CI)
numpy>=1.20.0
matplotlib>=3.4.0
pillow>=8.0.0

# Build dependencies
setuptools>=65.0.0
wheel>=0.37.0
